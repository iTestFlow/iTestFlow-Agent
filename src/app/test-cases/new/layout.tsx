import type { Metadata } from "next"

export const metadata: Metadata = { title: "Add New Test Case" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
