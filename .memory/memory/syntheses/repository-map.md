Repository map: `src/app/` owns Next.js pages and API routes; `src/modules/` owns product/domain workflows; `src/components/`, `src/lib/`, and `src/shared/` hold shared UI and infrastructure; `src/worker/` runs background work; `migrations/` owns PostgreSQL schema changes; `scripts/` owns repository utilities; `docs/`, `README.md`, and `PROJECT_ARCHITECTURE.md` document product and operations; colocated `*.test.ts`/`*.test.tsx` files plus Vitest configs provide verification.