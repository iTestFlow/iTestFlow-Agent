# Stack and tooling

Tooling and runtime signals inferred from manifests and config:
- Next.js is present.
- Package manager is inferred as npm from `package-lock.json`.
- Package name: `itestflow-agent`.
- TypeScript configuration is present.
- Useful package scripts include `typecheck`, `lint`, `test:coverage`, `test:coverage:all`, `test:coverage:integration`, `test:integration`, `test:model`, `test:unit`.
- Vitest is available for tests.

Update this synthesis when the language stack, runtime constraints, package manager, or verification tooling changes.
