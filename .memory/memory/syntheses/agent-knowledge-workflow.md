`AGENTS.md` is the repository-wide agent entrypoint. Before broad discovery, use native `.memory/` for durable project facts and the `itestflow_agent` MemPalace wing for indexed cross-session/project context; current files and tests always win conflicts. The tracked `.mcp.json` launches both stores, while `mempalace.yaml` owns taxonomy. Generated indexes, palace databases, secrets, sensitive logs, and task diaries stay untracked.