Route contract tests mock whole service modules. Any zod-schema constant or error class a route imports from a mocked service becomes undefined in tests (breaking schema construction and instanceof). Put limits, error classes, and pure types in a sibling .shared.ts module (see execution-test-data.shared.ts, execution-profiles.shared.ts) that routes and tests import unmocked, and have the server-only service re-export them.