Timestamp-style migration ids collide across branches: 1710000037000 exists twice (jira_oauth_identity and playwright_mcp_execution) and 38000-41000 belong to Jira migrations, so the Test Execution workbench migration is 1710000042000 and the next free id is 1710000043000. Separately, a database that ran migrations from a since-dropped branch fails node-pg-migrate's checkOrder for ANY new migration ('Not run migration X is preceding already run migration Y'); fix by deleting the dropped branch's rows from pgmigrations (orphaned tables can stay if their names cannot collide).