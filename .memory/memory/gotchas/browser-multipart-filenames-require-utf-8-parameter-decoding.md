Browser FormData sends non-ASCII filenames as UTF-8 bytes in an unextended multipart filename parameter, while Busboy defaults that parameter to Latin-1. Configure Busboy with defParamCharset set to utf8 at the streaming upload boundary or Arabic names will persist as mojibake such as Ø§Ù.... Keep a FormData regression test plus a DB-backed upload-to-context assertion for both documentName and originalFileName.