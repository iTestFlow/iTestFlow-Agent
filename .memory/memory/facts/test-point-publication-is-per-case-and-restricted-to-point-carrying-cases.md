playwright_execution_cases gained azure_plan_id so publication uses each case's own plan/suite/point identity (updateTestPoints), letting one run mix imports from different plans. publishableCases requires point, plan, and suite ids NOT NULL; manual and story-imported cases are simply not publication candidates and the old null-point failure-receipt branch was removed. Case retrieval by story reuses /api/azure-devops/linked-test-cases; plan/suite preview lives in execution-case-source.service.ts behind POST /api/test-execution/playwright/case-preview (read-only, no MCP/worker gates).