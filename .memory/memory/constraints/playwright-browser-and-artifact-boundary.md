Treat Playwright MCP browser state and artifact URLs as untrusted. Verify every reported tab against exact deployment origins before agent dispatch, after successful or rejected tool calls, and before completion; reject ambiguous reports before reuse or persistence. These checks are not an atomic network boundary, so deployment egress controls remain required. Artifact download queries are transient, URL userinfo and encoded path separators are rejected, and durable source provenance contains only origin and path.