Project source documents derive document_kind from the current version file_format: PNG, JPEG, and WebP are image; every other supported format is document. createDocumentWithVersion rejects a conflicting explicit kind, and createVersionForDocument updates document_kind atomically when it advances current_version_id. Preserve this invariant because project-knowledge source manifests consume document_kind.