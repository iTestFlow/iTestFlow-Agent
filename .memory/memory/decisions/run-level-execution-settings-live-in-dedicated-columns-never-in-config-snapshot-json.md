playwright_execution_runs gained base_url, execution_notes, screenshot_policy columns (migration 1710000042000_test_execution_workbench). They must NOT ride in config_snapshot_json: the worker hard-fails runs when that snapshot differs from live transport config by JSON equality (playwright-execution-job drift check), which must keep comparing only {transport, endpoint, artifactBaseUrl}.